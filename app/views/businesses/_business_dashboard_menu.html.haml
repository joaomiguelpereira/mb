%div.dashboard_header
  %div.title
    #{@business.short_name}
    
.context_horizontal_menu      
  .right_menu
    %ul
      %li
        =link_to "Informação", user_profile_path, :class=>(request.env['PATH_INFO'] == business_admin_business_path(@current_user, @business)) ? "selected" : ""
      %li
        =link_to "Alterar password", user_change_password_path, :class=>(request.env['PATH_INFO'] == user_change_password_path) ? "selected" : ""


    
  .left_menu
    %ul
      %li
        =link_to "Voltar ao dashboard", business_dashboard_path    
  
		
